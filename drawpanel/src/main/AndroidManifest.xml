<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gura.drawpanel">
<!--비용이 들거나, 배터리가 많이 들거나, 위치정보를 사용하거나, 연락처 정보, 저장장치 등등 .. 을 사용하겠다는 명시가 필요하다.(여기서)-->

    <!--외부 저장 장치(sdcard) 를 사용할 수 있는 허가-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>    <!-- write를 하면 read까지 가능, but read만 하면 write는 불가-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>